services:
  caddy:
    environment:
      BALLOON_CRUMBS_PREPRODUCTION_DOMAIN: ${BALLOON_CRUMBS_PREPRODUCTION_DOMAIN:?set BALLOON_CRUMBS_PREPRODUCTION_DOMAIN}
    volumes:
      - ./Caddyfile.preproduction:/etc/caddy/Caddyfile:ro
